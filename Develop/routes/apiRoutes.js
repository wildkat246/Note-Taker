const router = require('express').Router();
const path = require('path');

///Create a get routes for notes from database
router.get('/notes', (req, res) => {
    ///Write a funtion to get the notes from the function 

  });